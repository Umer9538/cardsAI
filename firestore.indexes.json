{
  "indexes": [
    {
      "_comment": "Loose token search. array-contains-any returns an arbitrary page, so the query orders by rank to pull generic reference foods ahead of restaurant dishes.",
      "collectionGroup": "foods",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "tokens", "arrayConfig": "CONTAINS" },
        { "fieldPath": "rank", "order": "ASCENDING" }
      ]
    },
    {
      "_comment": "Multi-word search by sorted word pair. A pair requires both words, which is what stops 'olive oil' matching OLIVE GARDEN.",
      "collectionGroup": "foods",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "pairs", "arrayConfig": "CONTAINS" },
        { "fieldPath": "rank", "order": "ASCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
