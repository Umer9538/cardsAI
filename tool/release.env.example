# Copy to tool/release.env and fill in. That file is gitignored: these are
# account identifiers, not secrets, but they are not useful to anyone else.
WORKER_URL=https://carbsai-api.quranai.workers.dev

# From AdMob → Apps → <app> → Ad units. NOT the application id.
ADMOB_REWARDED_ANDROID=
ADMOB_APP_OPEN_ANDROID=
ADMOB_REWARDED_IOS=
ADMOB_APP_OPEN_IOS=
